getting a bash shell

  docker run debian:jessie
  docker run --name jessie -rm debian:jessie sleep 1234
  docker run -ti debian:jessie bash

copying files in and out if containers

  docker cp test_file jessie:/tmp
  docker cp jessie:/etc/hosts ./

mounting directories
  docker run --rm -v `pwd`/dir:/mountted-dir -ti debian:jessie bash
  docker run --rm -v `pwd`/dir:/mountted-dir -ti debian:jessie bash

docker managed volumes
  docker volume ls
  docker volume create data
  docker run --rm -v data:/mountted-dir -ti debian:jessie bash
  touch /mountted-dir/hi
  docker run --rm -v data:/mountted-dir -ti debian:jessie bash
  docker volume rm data

docker kill db-server
docker rm db-server
